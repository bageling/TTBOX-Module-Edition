# TTBOX 代码资产盘点（2026-09-13）

## 1. 最新完整代码在哪

权威代码在本地仓库 `main` 分支：

`C:/Users/Administrator/Desktop/TTBOX-Module-Edition`

| 模块 | 本地路径 | 文件数 | 代码行数（不含空白统计） |
| --- | --- | --- | --- |
| Core C++ | `core/src` | 150 | 约 21,000 |
| Core 测试/工具 | `core/tests` + `core/tools` | 112 | 约 14,300 |
| Web 插件 | `plugins/web` | 17 | 约 21,000 |
| 其它插件 | `plugins/*` | 16 | 约 7,000 |
| 系统脚本 | `scripts` | 25 | 约 7,500 |
| USB Proxy | `usbproxy` | 20 | 约 5,000 |
| 框架 | `framework` | 27 | 约 1,600 |
| 模型转换/训练 | `tools/converter` + `ttbox_motion` | 4 | 约 2,000 |

板端真正运行的不是整个仓库，而是三套东西：

1. Core 主程序：`/opt/ttbox/bin/ttbox_core_main`
2. Web 服务：`/opt/ttbox/scripts/ttbox_web.py`（`plugins/web/bin/ttbox-web` 只是启动壳）
3. USB Proxy：`/opt/ttbox/usbproxy/usb-proxy`

## 2. 与板端一致性（MD5 核对）

### 已一致（可以放心）

| 文件 | 本地 MD5 | 板端 MD5 |
| --- | --- | --- |
| `plugins/web/bin/ttbox-web.py` | `4b61fc21...` | 相同 |
| `scripts/ttbox_web.py` | `4b61fc21...` | 相同 |
| `plugins/web/static/app.js` | `e9b3a30a...` | 相同 |
| `core/src/**`（全部共享文件） | 与板端逐文件一致 | 一致 |
| `scripts/edid/**` | 与板端 `/opt/ttbox/scripts/edid` 一致 | 一致 |
| `scripts/update_engine.py` | 一致 | 一致 |
| `scripts/wifi_manager.py` | 内容一致（仅换行符差异） | 一致 |
| Core 二进制 | 板端构建 `b9b1fd2a...` | 板端安装相同 |

### 不一致（需要处理）

| 文件 | 本地 | 板端 | 影响 |
| --- | --- | --- | --- |
| `usbproxy/device-libusb.cpp` | 新（9/13 12:39） | 旧（9/5 17:22） | 板端源码旧，但运行中的 `usb-proxy` 是新二进制，源码需回写对齐 |
| `core/CMakeLists.txt` | 新 | 旧 | 板端测试注册缺新用例，不影响运行 |
| `core/tests/*` 若干 | 新 | 旧 | 板端测试套件过期，不影响运行 |
| `config/default.json` | 模板 | 运行态配置 | 正常差异，板端多了运行时参数 |
| `config/hardware_display.json` | 模板 | 真实显示器身份 | 正常差异 |

### 板端过期冗余（可清理/已不用）

- `/opt/ttbox/src/scripts/ttbox_web.py`、`update_engine.py`、`edid/*`：旧快照，活动目录是 `/opt/ttbox/scripts`。
- `/opt/ttbox/src/build/*`：旧构建产物（815616 字节），当前构建在 `/opt/ttbox/src/core/build`（847288 字节）。
- `/opt/ttbox/bin/ttbox_core_main.bak*` 40+ 个旧二进制。
- `/opt/ttbox/src/core.bak-*`、`core.positive`、`core.stage2` 等旧源码快照。
- 板端源码里残留的旧文件：`core/src/rknn/CoreRuntime.cpp`、`aim/AlphaBetaGammaFilter.hpp`、`mouse/Smooth.hpp`、各 `.bak-*` 文件。

## 3. 测试资产盘点

### 有用且在用

- `ttbox_core_tests` 聚合测试：含 IPC、模型注册、解码、鼠标、RuntimeProfile 等 17 组。
- 25 个独立 CTest 用例：AimThread、Tracker、Selector 压力、Recoil、Pipeline、Decoder、Hotkey、Output、RKNN 故障注入等。
- 板端硬件专用用例：`test_capture_hw`、`test_rga_hw`、`test_rknn_hw`、`test_hid_forward_hw`、`test_model_switch_hw` 等。
- 调试工具（保留）：`usbproxy_ping.py`、`usbproxy_state.py`、`usbproxy_buttontest.py`、`monitor_board_runtime.py`、`fetch_state_metrics.py`、`ipc_probe*.py`、`trace_target.py`、`pid_sim/*`。
- Python 框架测试：`framework/tests/*`（8 个），其中 `test_web_plugin.py` 今日已更新。

### 已作废/不再接线

- `core/tests/test_target_selector_mt.cpp`：没有进入 CMake，已被 `test_selector_stress` 取代。
- 板端 CTest 里旧注册：`test_hotkey_gate`、`test_metrics_g1` 等已从本地注册列表移除（但源码仍在聚合测试里维护）。
- 一次性实验脚本（不再维护）：`fullscreen_*.py`、`s1_metrics.py`、`s2_deep.py`、`s2_telemetry.py`、`s6_final.py`、`stab.py`、`samp.py`、`stable_check.py`、`status1.py`、`final_check.py`、`safety_check.py`、`hf_sweep.py`、`enable_inject.py`、`toggle_inject.py`、`set_real_hotkey.py` 等。
- Windows 调试用 C# 片段：`CenterImage.cs`、`CursorPoll.cs`、`FullscreenImage.cs`、`GetPos.cs`、`SetPos.cs`、`MouseHook.cs`、`HidProbe.cs`（仅开发用，可归档）。
- `tests/monitor/*`、`tests/api/*.sh`：TTBOX 链路/接口冒烟脚本，保留作参考。

## 4. 建议的收尾动作

1. 把本地 `usbproxy/device-libusb.cpp` 回写板端 `/opt/ttbox/src/usbproxy/`，让源码和运行二进制一致。
2. 同步板端 `core/CMakeLists.txt` 和 `core/tests/*`，恢复板端新测试套件。
3. 板端冗余 `.bak*` 和旧 `src/build` 归档到一个只读备份目录，避免误用。
4. 板端 `src/scripts` 旧快照可直接删除或用软链指向 `/opt/ttbox/scripts`。
